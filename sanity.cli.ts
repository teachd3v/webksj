import { defineCliConfig } from 'sanity/cli';

export default defineCliConfig({
    api: {
        projectId: 's9dsj8ot',
        dataset: 'production',
    },
    deployment: {
        appId: 'z996yhdk83ix3t73s28jdhlg',
    },
});
